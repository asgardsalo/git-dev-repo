openapi: 3.0.0
info:
  title: Hello World API
  description: API Proxy for Hello World
  version: 1.0.0
servers:
  - url: ${apiEndpoint}
paths:
  ${apiBasePath}:
    get:
      summary: Returns a hello world message
      responses:
        '200':
          description: Successful response